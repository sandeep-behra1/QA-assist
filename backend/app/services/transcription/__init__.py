"""Speech-to-text: audio in, canonical transcript out.

Import the providers and service from their modules
(``app.services.transcription.providers`` / ``.service``); this package
init is intentionally empty so ``wav`` helpers can be imported by providers
without a cycle.
"""
